First, there is no operator `+` for two iterators. Second, for arithmetic types, using `mid = (beg + end) / 2` may lead to overflow.
> Yeah I was awared of the second reason but not the first one. Indeed there is no + opeartor for two iterators, only -, self increment or decrement.